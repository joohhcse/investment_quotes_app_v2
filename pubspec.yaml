name: investment_quotes_app_v2
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+10

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

#  google_mobile_ads: ^3.1.0 # 파이어베이스 애드몹 패키지
  google_mobile_ads: ^2.3.0 # 책: 코드팩토리 플러터 프로그래밍
#  google_mobile_ads: ^0.11.0+4
  shared_preferences: ^2.0.13 # shared_preferences
  sqflite: ^2.0.0+3
  path: ^1.8.1
  provider: 6.0.3
  # sqflite_common_ffi: any
  flutter_launcher_icons: "^0.13.1"

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:

  uses-material-design: true

